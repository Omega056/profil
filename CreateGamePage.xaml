<Page x:Class="WpfApp14.CreateGamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:WpfApp14"
      Title="Создать игру">
    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#E0CDA5" Offset="0"/>
            <GradientStop Color="#F0E0C0" Offset="1"/>
        </LinearGradientBrush>
    </Page.Background>

    <Page.Resources>
        <!-- Конвертер для управления шириной панели -->
        <local:BooleanToGridLengthConverter x:Key="BooleanToGridLengthConverter"/>

        <!-- Тень для карточек и панелей -->
        <DropShadowEffect x:Key="CardShadow" ShadowDepth="3" BlurRadius="8" Color="#888888" Opacity="0.4"/>

        <!-- Эффект стекла -->
        <BlurEffect x:Key="GlassEffect" Radius="10"/>

        <!-- Прозрачные скруглённые кнопки -->
        <Style TargetType="Button" x:Key="RoundedButton">
            <Setter Property="Background" Value="#E2C49C"/>
            <Setter Property="BorderBrush" Value="#C2A47C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15"
                                Effect="{StaticResource CardShadow}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                        To="1.05" Duration="0:0:0.2" />
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                        To="1.05" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                        To="1" Duration="0:0:0.2" />
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                        To="1" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseDown">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                        To="0.95" Duration="0:0:0.1" AutoReverse="True" />
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                        To="0.95" Duration="0:0:0.1" AutoReverse="True" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Новый стиль для миниатюрных кнопок -->
        <Style TargetType="Button" x:Key="MiniRoundedButton" BasedOn="{StaticResource RoundedButton}">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="6,3"/>
        </Style>

        <Style TargetType="Button" x:Key="LargeRoundedButton" BasedOn="{StaticResource RoundedButton}">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>

        <!-- Стиль для прозрачных скруглённых текстовых полей -->
        <Style TargetType="TextBox" x:Key="RoundedTransparentTextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#A07050"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для ComboBox -->
        <Style TargetType="ComboBox" x:Key="ButtonStyledComboBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#A07050"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15"
                                Effect="{StaticResource CardShadow}">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton"
                                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            BorderThickness="0"/>
                                <ContentPresenter x:Name="ContentSite"
                                                IsHitTestVisible="False"
                                                Content="{TemplateBinding SelectionBoxItem}"
                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                Margin="8,4"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left"/>
                                <TextBox x:Name="PART_EditableTextBox"
                                        Style="{StaticResource RoundedTransparentTextBox}"
                                        Visibility="Hidden"
                                        IsReadOnly="{TemplateBinding IsReadOnly}"/>
                                <Popup x:Name="PART_Popup"
                                    IsOpen="{TemplateBinding IsDropDownOpen}"
                                    Placement="Bottom"
                                    AllowsTransparency="True"
                                    Focusable="False"
                                    PopupAnimation="Slide">
                                    <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        Background="Transparent">
                                        <Border x:Name="DropDownBorder"
                                                Background="Transparent"
                                                BorderBrush="#A07050"
                                                BorderThickness="2"
                                                CornerRadius="5"/>
                                        <ScrollViewer Margin="4,6,4,6"
                                                    Background="Transparent">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70*"/>
            <ColumnDefinition x:Name="AIChatPanel" Width="{Binding IsChatPanelOpen, Converter={StaticResource BooleanToGridLengthConverter}, ConverterParameter=30}"/>
        </Grid.ColumnDefinitions>

        <!-- Главное содержимое -->
        <DockPanel Grid.Column="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" DockPanel.Dock="Top">
                <StackPanel>
                    <!-- Название викторины и количество вопросов -->
                    <Border Background="#C2A47C" CornerRadius="15" Padding="15" Opacity="0.95" Margin="0,0,0,15" Effect="{StaticResource CardShadow}">
                        <StackPanel>
                            <TextBlock Text="Название викторины:" FontSize="16" FontWeight="SemiBold" Foreground="#333333"/>
                            <TextBox x:Name="QuizTitleBox" Height="30" Margin="0,5,0,15" Style="{StaticResource RoundedTransparentTextBox}"/>

                            <TextBlock Text="Количество вопросов:" FontSize="16" FontWeight="SemiBold" Foreground="#333333"/>
                            <ComboBox x:Name="QuestionsCountCombo" Width="80" SelectedIndex="4"
                                      SelectionChanged="QuestionsCountCombo_SelectionChanged" Margin="0,5,0,0"
                                      Style="{StaticResource ButtonStyledComboBox}">
                                <ComboBoxItem>1</ComboBoxItem>
                                <ComboBoxItem>2</ComboBoxItem>
                                <ComboBoxItem>3</ComboBoxItem>
                                <ComboBoxItem>4</ComboBoxItem>
                                <ComboBoxItem>5</ComboBoxItem>
                                <ComboBoxItem>6</ComboBoxItem>
                                <ComboBoxItem>7</ComboBoxItem>
                                <ComboBoxItem>8</ComboBoxItem>
                                <ComboBoxItem>9</ComboBoxItem>
                                <ComboBoxItem>10</ComboBoxItem>
                                <ComboBoxItem>11</ComboBoxItem>
                                <ComboBoxItem>12</ComboBoxItem>
                                <ComboBoxItem>13</ComboBoxItem>
                                <ComboBoxItem>14</ComboBoxItem>
                                <ComboBoxItem>15</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                    <!-- Вопросы -->
                    <ItemsControl x:Name="QuestionsItemsControl" ItemsSource="{Binding Questions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#C2A47C" CornerRadius="12" Padding="12" Margin="0,0,0,15" Opacity="0.95" Effect="{StaticResource CardShadow}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding QuestionHeader}" FontWeight="Bold" FontSize="16" Foreground="#333333"/>
                                        <TextBox Text="{Binding QuestionText, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Style="{StaticResource RoundedTransparentTextBox}"/>

                                        <ItemsControl ItemsSource="{Binding Answers}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                                        <RadioButton IsChecked="{Binding IsCorrect}" GroupName="{Binding GroupName}" Margin="0,0,5,0"/>
                                                        <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Width="250" Style="{StaticResource RoundedTransparentTextBox}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <TextBlock Text="Таймер:" Width="60" FontSize="14" Foreground="#333333"/>
                                            <ComboBox SelectedIndex="{Binding SelectedTimerIndex}" Width="100" Style="{StaticResource ButtonStyledComboBox}">
                                                <ComboBoxItem>10 сек</ComboBoxItem>
                                                <ComboBoxItem>20 сек</ComboBoxItem>
                                                <ComboBoxItem>30 сек</ComboBoxItem>
                                            </ComboBox>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Кнопки внизу -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                        <Button Content="← Назад"
                                Style="{StaticResource RoundedButton}"
                                Width="80"
                                Click="BackButton_Click"
                                Margin="0,0,10,0"/>
                        <Button Content="Помощь ИИ"
                                Style="{StaticResource MiniRoundedButton}"
                                Click="ToggleAIChatButton_Click"
                                Margin="0,0,10,0"/>
                        <Button Content="Добавить викторину"
                                Style="{StaticResource LargeRoundedButton}"
                                Click="AddQuizButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>

        <!-- Панель чата с ИИ -->
        <Border Grid.Column="1" Background="#E2C49C" CornerRadius="15" Margin="10,0,0,0" Effect="{StaticResource CardShadow}" Opacity="0.95">
            <Border.RenderTransform>
                <TranslateTransform x:Name="ChatPanelTransform" X="100"/>
            </Border.RenderTransform>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="ChatPanelTransform"
                                            Storyboard.TargetProperty="X"
                                            From="100" To="0" Duration="0:0:0.3"
                                            BeginTime="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
            <DockPanel>
                <TextBlock Text="Чат с ИИ" FontSize="18" FontWeight="Bold" Foreground="#333333" HorizontalAlignment="Center" Margin="0,10,0,15" DockPanel.Dock="Top"/>
                <ListView ItemsSource="{Binding ChatMessages}" DockPanel.Dock="Top" Margin="10" Background="Transparent" BorderThickness="0" Height="400">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Sender}" FontWeight="Bold" Foreground="#333333" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Foreground="#333333"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" Margin="10">
                    <TextBox x:Name="ChatInputBox" Width="180" Style="{StaticResource RoundedTransparentTextBox}" Margin="0,0,8,0"/>
                    <Button Content="Отправить" Style="{StaticResource MiniRoundedButton}" Click="SendMessageButton_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Page>