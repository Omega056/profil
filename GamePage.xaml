<Page x:Class="WpfApp14.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="GamePage">
    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#D2B48C" Offset="0"/>
            <GradientStop Color="#E0CDA5" Offset="1"/>
        </LinearGradientBrush>
    </Page.Background>

    <Page.Resources>
        <!-- Тень для кнопок и карточек -->
        <DropShadowEffect x:Key="DropShadow" ShadowDepth="5" BlurRadius="15" Color="#666666" Opacity="0.7"/>

        <!-- Стили кнопок -->
        <Style TargetType="Button" x:Key="RoundedButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20"
                                Effect="{StaticResource DropShadow}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                        To="1.05" Duration="0:0:0.2" />
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                        To="1.05" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                        To="1" Duration="0:0:0.2" />
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                        To="1" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseDown">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                        To="0.95" Duration="0:0:0.1" AutoReverse="True" />
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                        To="0.95" Duration="0:0:0.1" AutoReverse="True" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="AnswerButton" BasedOn="{StaticResource RoundedButton}">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="MaxWidth" Value="500"/>
        </Style>

        <Style TargetType="Button" x:Key="QuitButton" BasedOn="{StaticResource RoundedButton}">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#FF4444"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Page.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Вопрос -->
        <Border Grid.Row="0" CornerRadius="20" Padding="20" Margin="0,0,0,40" Effect="{StaticResource DropShadow}" Opacity="0.95">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#D2B48C" Offset="0"/>
                    <GradientStop Color="#C9A674" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <TextBlock x:Name="QuestionTextBlock"
                       FontSize="26"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       Foreground="#222222"
                       HorizontalAlignment="Center" />
        </Border>

        <!-- Ответы -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button x:Name="AnswerButton1" Style="{StaticResource AnswerButton}" Click="OnAnswer_Click" Width="192" Background="#FF562C2C" />
            <Button x:Name="AnswerButton2" Style="{StaticResource AnswerButton}" Click="OnAnswer_Click" Background="#FF562C2C" />
            <Button x:Name="AnswerButton3" Style="{StaticResource AnswerButton}" Click="OnAnswer_Click" Background="#FF562C2C" />
            <Button x:Name="AnswerButton4" Style="{StaticResource AnswerButton}" Click="OnAnswer_Click" Background="#FF562C2C" />
        </StackPanel>

        <!-- Таймер и выход -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Border CornerRadius="15" Padding="10,5" Effect="{StaticResource DropShadow}" Opacity="0.95">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#D2B48C" Offset="0"/>
                        <GradientStop Color="#C9A674" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Осталось времени:" FontSize="18" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,8,0" />
                    <TextBlock x:Name="TimerTextBlock" FontSize="18" Foreground="#333333" />
                </StackPanel>
            </Border>
            <Button Content="Выйти" Style="{StaticResource QuitButton}" Click="OnQuit_Click" Margin="20,0,0,0" Background="#FF461313" />
        </StackPanel>
    </Grid>
</Page>