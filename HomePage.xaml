<Page x:Class="WpfApp14.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Главная">
    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF99A8" Offset="0"/>
            <GradientStop Color="#FFC1CC" Offset="1"/>
        </LinearGradientBrush>
    </Page.Background>

    <Page.Resources>
        <!-- Стили кнопок -->
        <Style TargetType="Button" x:Key="RoundedButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20"
                                Effect="{DynamicResource ButtonShadow}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseDown">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                        To="0.95" Duration="0:0:0.1" AutoReverse="True"/>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                        To="0.95" Duration="0:0:0.1" AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="LargeRoundedButton" BasedOn="{StaticResource RoundedButton}">
            <Setter Property="Height" Value="55"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <!-- Тень для кнопок -->
        <DropShadowEffect x:Key="ButtonShadow" ShadowDepth="4" BlurRadius="10" Color="#888888" Opacity="0.5"/>

        <!-- Эффект стекла для других элементов -->
        <BlurEffect x:Key="GlassEffect" Radius="12" KernelType="Gaussian"/>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Grid Grid.Row="0" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Background="Transparent" BorderThickness="0" Click="SettingsButton_Click">
                <Image Source="/1486395874-settings_80622.png" Width="35" Height="35"/>
            </Button>
            <TextBlock Grid.Column="1"/>
            <Button Grid.Column="2" Background="Transparent" BorderThickness="0" Click="StoreButton_Click">
                <Image Source="/3986728-online-shop-store-store-icon_112980.png" Width="35" Height="35"/>
            </Button>
        </Grid>

        <!-- Профиль + ежедневное испытание -->
        <StackPanel Grid.Row="1" Margin="0,30,0,30">
            <Border x:Name="ProfileBorder" CornerRadius="25" Background="White" Padding="20" Margin="0,0,0,25" Opacity="0.85" MouseLeftButtonDown="ProfileBorder_MouseLeftButtonDown" Cursor="Hand">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Ellipse Width="70" Height="70" Fill="#FFC1CC"/>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0">
                        <TextBlock x:Name="ProfileUsernameTextBlock" Text="Студент" FontWeight="Bold" FontSize="18" Foreground="#333333"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="IQ: " FontWeight="SemiBold" FontSize="14" Foreground="#666"/>
                            <TextBlock x:Name="ProfileIQTextBlock" Text="100" FontSize="14" Foreground="#666"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="Правильных ответов: " FontWeight="SemiBold" FontSize="14" Foreground="#666"/>
                            <TextBlock x:Name="ProfileCorrectAnswersTextBlock" Text="0%" FontSize="14" Foreground="#666"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
            <Border CornerRadius="25" Background="White" Padding="20" Opacity="0.85">
                <StackPanel>
                    <TextBlock Text="Ежедневное испытание" FontWeight="Bold" FontSize="16" Foreground="#333"/>
                    <TextBlock Text="Дойти до: $2,000" FontSize="14" Foreground="#888" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Нижние кнопки -->
        <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
            <Button x:Name="CreateGameButton"
                    Content="Создать игру"
                    Style="{StaticResource LargeRoundedButton}"
                    Click="CreateGameButton_Click"
                    Margin="0,0,0,15"/>
            <Button x:Name="FindGameButton"
                    Content="Найти игру"
                    Style="{StaticResource LargeRoundedButton}"
                    Click="FindGameButton_Click"
                    Margin="0,0,0,15"/>
            <Button x:Name="LogoutButton"
                    Content="Выйти"
                    Style="{StaticResource LargeRoundedButton}"
                    Click="LogoutButton_Click"/>
        </StackPanel>
    </Grid>
</Page>